<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ButtonOutline" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/white</item> <!-- Color del texto -->
        <item name="cornerRadius">16dp</item> <!-- Esquinas redondeadas -->
        <item name="strokeColor">@android:color/white</item> <!-- Color del borde -->
        <item name="android:backgroundTint">@android:color/transparent</item> <!-- Fondo transparente -->
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>


    </style>


</resources>